# Design System

<!-- impeccable:design-schema 1 -->

## Survey Question Semantics

- 题型标签使用小型圆角矩形，不取代题目标题：单选与矩阵单选为淡黄色，多选与矩阵多选为淡红色，简答为淡蓝色，个人信息为淡绿色，文件为淡紫色。
- “必答”使用站点强调色的低饱和背景；考试分数使用独立紫色状态，不与题型颜色混用。
- 标签文字必须在浅色、深色与自定义强调色主题下保持足够对比度，不只依赖颜色传达含义。

## Survey Answer Sheet

- 大于 900px 时，答题卡固定在问卷右侧并随页面滚动；主表单保持独立阅读列。
- 900px 及以下时，答题卡入口固定在左下角，避免与右下角全站“外观”入口冲突；点击后从右侧打开不透明抽屉和遮罩。
- 答题卡必须显示已答数量、进度条以及已答/未答状态；题目逻辑改变时只列出当前可见题。
- 移动端触控目标至少 40px，抽屉不得造成页面横向溢出，并提供明确关闭按钮。

## Exam States

- 考试倒计时使用等宽数字；剩余时间少于五分钟时切换为警示色，但仍保留“剩余时间”文字。
- 得分属于结果状态，使用大号实色数字与较弱的满分说明；公开作答界面不展示答案键。
- 候场页是考试与答题之间的硬边界：说明卡、开放时间、确认勾选和开始按钮依次出现；题目及答题卡在取得服务端凭证前不得渲染。
- 人工评分输入紧邻对应简答内容，显示本题满分；存在未批改题时，提交者看到“客观题暂得分”而不是最终成绩。

## Campus Food Rankings

- 红榜以低饱和绿色边框、背景和状态标签呈现；黑榜以低饱和红色呈现。文字必须同时写明“红榜推荐”或“黑榜避雷”，不能只依赖颜色。
- 搜索、红黑榜筛选和分类筛选位于卡片网格上方；桌面双列、移动端单列，每家餐厅始终是一张独立模块卡。
- 管理端沿用现有表单与卡片尺度，榜单类型使用明确的二选一控件；公开页面不得出现编辑入口。

## Admin Survey Configuration

- 问卷类型采用三选一选择卡：普通问卷、考试、信息查询。
- 正确答案、分数和条件逻辑使用统一的浅色设置面板，紧邻对应题目，不另开多层弹窗。
- 条件逻辑只允许引用前方选择题，移动题目导致引用失效时应自动清除条件。

## Information Feedback

- 提交者反馈由可重复模块组成，语义状态固定为 neutral、positive、warning。
- 公开查询凭证仅允许邮箱、学号/工号或身份证个人信息题，并对 IP 与重复失败凭证限流；姓名等低熵字段不得单独作为查询凭证。
- 三种状态使用轻背景与细边框区分，禁止仅使用单侧粗色条；模块始终显示标题和正文。
- 后台编辑与公开查询沿用站点现有浅紫、玻璃质感和圆角尺度，不创建新的视觉品牌。
